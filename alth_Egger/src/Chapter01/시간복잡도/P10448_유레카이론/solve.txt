
삼각수를 구하는 방법
- 브루드 포스트로 구하기 -

그냥 쌩 구현.
    -> n(n+1)/2 의 값이 n의 삼각형을 공을 다 더한 공식 (N까지의 합을 더한 값), 그리고 그 값 들 중에서
    3중 for문을 주구장창 돌려서 3개의 값을 더 했을 경우에 주어진 값과 동일하면 ==] 삼각수임을 나타내기에 1을
    return한다.

    반복문에는 ~까지 돌려라 라는 조건을 바로 적지 않고

     for (int j = 1; ; j++) {
                    int triangularNumber = (j * (j + 1)) / 2;
                    if (triangularNumber > solveNum) break;
                    list.add(triangularNumber);
                }

     N까지의 합이 주어지는 전체 삼각형에 들어가는 공의 개수 보다 커질때 까지만 반복문을 돌린다. 어차피
     주어지는 공의 개수 보다 커지면 그걸로는 3번 더할 수 없기 때문이다.

     이게 시간초과가 안나는게 신기하네 (N^3) 이 나오는거 아닌가